# =============================================================================
# Ollama Cloud API Configuration
# (Shared by opencode-ai and hermes-agent)
# =============================================================================
# opencode: uses the built-in ollama-cloud provider — OLLAMA_API_KEY only.
# hermes: uses the built-in ollama-cloud provider — OLLAMA_API_KEY only.
# =============================================================================
OLLAMA_API_KEY=
